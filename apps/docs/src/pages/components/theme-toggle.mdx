import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { ThemeToggle, HStack, VStack, Text } from '@cumulo/core';
import themeToggleDoc from '../../../docgen/components/ThemeToggle.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# ThemeToggle

ThemeToggle provides seamless theme switching between `light`, `dark`, and `system` modes with zero flash of unstyled content (FOUC) and hydration matching.

```tsx
import { ThemeToggle } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview
  title="Theme Toggle Variations"
  description="Supports 2-state smart toggle mode or 3-state cycling."
  code={`<HStack gap="lg" align="center" wrap="wrap">
  <ThemeToggle size="base" variant="outline" mode="toggle" />
  <ThemeToggle size="base" variant="secondary" mode="cycle" showLabel />
  <ThemeToggle size="small" variant="ghost" shape="default" showLabel />
</HStack>`}
>
  <HStack gap="lg" align="center" wrap="wrap">
    <ThemeToggle size="base" variant="outline" mode="toggle" />
    <ThemeToggle size="base" variant="secondary" mode="cycle" showLabel />
    <ThemeToggle size="small" variant="ghost" shape="default" showLabel />
  </HStack>
</ComponentPreview>

---

## Component API

<PropsTable data={themeToggleDoc} />
