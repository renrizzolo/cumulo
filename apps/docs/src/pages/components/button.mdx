import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Button, HStack, VStack, Badge } from '@cumulo/core';
import buttonDoc from '../../../docgen/components/Button.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Button

Buttons allow users to take actions, make choices, and trigger state changes with a single tap or click.

```tsx
import { Button } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview
  title="Button Story"
  description="Explore primary, secondary, outline, and ghost variants across intents."
  code={`<HStack gap="sm" wrap="wrap" align="center">
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline" intent="success">Outline Success</Button>
  <Button variant="ghost" intent="error">Ghost Error</Button>
  <Button size="small" variant="primary" shape="round">Round Small</Button>
</HStack>`}
>
  <HStack gap="sm" wrap="wrap" align="center">
    <Button variant="primary">Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline" intent="success">
      Outline Success
    </Button>
    <Button variant="ghost" intent="error">
      Ghost Error
    </Button>
    <Button size="small" variant="primary" shape="round">
      Round Small
    </Button>
  </HStack>
</ComponentPreview>

---

## Variants

Cumulo buttons support four core visual styles: `primary`, `secondary`, `outline`, and `ghost`.

<ComponentPreview
  title="Visual Variants"
  code={`<HStack gap="sm" wrap="wrap">
  <Button variant="primary">Primary</Button>
  <Button variant="secondary">Secondary</Button>
  <Button variant="outline">Outline</Button>
  <Button variant="ghost">Ghost</Button>
</HStack>`}
>
  <HStack gap="sm" wrap="wrap">
    <Button variant="primary">Primary</Button>
    <Button variant="secondary">Secondary</Button>
    <Button variant="outline">Outline</Button>
    <Button variant="ghost">Ghost</Button>
  </HStack>
</ComponentPreview>

---

## Semantic Intents

Buttons map across system intents (`primary`, `success`, `warning`, `error`, `info`).

<ComponentPreview
  title="Intents"
  code={`<HStack gap="sm" wrap="wrap">
  <Button variant="primary" intent="primary">Primary</Button>
  <Button variant="primary" intent="success">Success</Button>
  <Button variant="primary" intent="warning">Warning</Button>
  <Button variant="primary" intent="error">Error</Button>
  <Button variant="primary" intent="info">Info</Button>
</HStack>`}
>
  <HStack gap="sm" wrap="wrap">
    <Button variant="primary" intent="primary">
      Primary
    </Button>
    <Button variant="primary" intent="success">
      Success
    </Button>
    <Button variant="primary" intent="warning">
      Warning
    </Button>
    <Button variant="primary" intent="error">
      Error
    </Button>
    <Button variant="primary" intent="info">
      Info
    </Button>
  </HStack>
</ComponentPreview>

---

## Sizes & Shapes

Buttons offer `small`, `base`, and `large` sizes, with `default` and `round` shapes.

<ComponentPreview
  title="Sizes & Shapes"
  code={`<HStack gap="sm" align="center" wrap="wrap">
  <Button size="small">Small</Button>
  <Button size="base">Base</Button>
  <Button size="large">Large</Button>
  <Button shape="round" variant="secondary">Round Pill</Button>
  <Button disabled variant="primary">Disabled</Button>
</HStack>`}
>
  <HStack gap="sm" align="center" wrap="wrap">
    <Button size="small">Small</Button>
    <Button size="base">Base</Button>
    <Button size="large">Large</Button>
    <Button shape="round" variant="secondary">
      Round Pill
    </Button>
    <Button disabled variant="primary">
      Disabled
    </Button>
  </HStack>
</ComponentPreview>

---

## Component API

<PropsTable data={buttonDoc} />
