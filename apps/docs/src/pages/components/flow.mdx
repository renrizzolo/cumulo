import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Flow, Heading, Text, Button } from '@cumulo/core';
import flowDoc from '../../../docgen/components/Flow.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Flow

Vertical text flow and rhythm primitive based on Andy Bell's lobotomized owl flow pattern (`.flow > * + *`). Automatically spaces sequential sibling elements using CSS custom properties (`--flow-space`) with sensible relative defaults (`1em`).

```tsx
import { Flow } from '@cumulo/core';
```

## Flow

Flow can be used to create vertical text flow with different spacing. Use `md`, `lg`, or `xl` to control the spacing between elements. The space before headings (h2, h3, h4) is larger, creating natural sections.

<ComponentPreview>
  <Flow>
    <Heading as="h2">Section Header</Heading>
    <Text>
      This paragraph directly follows a heading, so flow space is automatically tightened.
    </Text>
    <Text>Subsequent paragraphs maintain standard editorial rhythm and readability.</Text>
    <Text>
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatibus, maxime. Quisquam, quod.
      Harum, soluta.
    </Text>
    <Heading as="h4">Another Section Header</Heading>
    <Text size="sm">
      This paragraph directly follows a heading, so flow space is automatically tightened.
    </Text>
    <Text size="sm">Subsequent paragraphs maintain standard editorial rhythm and readability.</Text>
    <Text size="sm">
      Lorem ipsum dolor sit amet consectetur adipisicing elit. Voluptatibus, maxime. Quisquam, quod.
      Harum, soluta.
    </Text>
  </Flow>
</ComponentPreview>

## Component API

<PropsTable data={flowDoc} />
