import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Surface, VStack, Text } from '@cumulo/core';
import surfaceDoc from '../../../docgen/components/Surface.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Surface

Contextual surface container that coordinates depth levels (0, 1, 2) and automatically adapts child tokens.

```tsx
import { Surface } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview
  code={`<Surface level={0} padding="lg">
  <VStack gap="md">
    <Text type="label">Surface Level 0 (Canvas)</Text>
    <Surface level={1} padding="md">
      <VStack gap="sm">
        <Text type="label">Surface Level 1 (Card)</Text>
        <Surface level={2} padding="sm">
          <Text type="label">Surface Level 2 (Elevated)</Text>
        </Surface>
      </VStack>
    </Surface>
  </VStack>
</Surface>`}
>
  <Surface level={0} padding="lg">
    <VStack gap="md">
      <Text type="label">Surface Level 0 (Canvas)</Text>
      <Surface level={1} padding="md">
        <VStack gap="sm">
          <Text type="label">Surface Level 1 (Card)</Text>
          <Surface level={2} padding="sm">
            <Text type="label">Surface Level 2 (Elevated)</Text>
          </Surface>
        </VStack>
      </Surface>
    </VStack>
  </Surface>
</ComponentPreview>

## Component API

<PropsTable data={surfaceDoc} />
