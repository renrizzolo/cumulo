import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Field, Container } from '@cumulo/core';
import fieldDoc from '../../../docgen/components/Field.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Field

Accessible compound form field coordinating labels, inputs, descriptions, and error states.

```tsx
import { Field } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview>
  <Container size="sm" padding="none">
    <Field.Root intent="primary">
      <Field.Label>Work Email</Field.Label>
      <Field.Input placeholder="you@company.com" />
      <!-- this comment is here to keep the text on one line, to prevent mdx wrapping it in an extra p tag -->
      <Field.Description>We'll only use this for critical workspace notifications.</Field.Description>
    </Field.Root>
  </Container>
</ComponentPreview>

## Invalid State

<ComponentPreview>
  <Container size="sm" padding="none">
    <Field.Root isInvalid>
      <Field.Label>Username</Field.Label>
      <Field.Input value="invalid_user@" readOnly />
      <Field.Error>Username cannot contain special symbols.</Field.Error>
    </Field.Root>
  </Container>
</ComponentPreview>

## Component API

<PropsTable data={fieldDoc} />
