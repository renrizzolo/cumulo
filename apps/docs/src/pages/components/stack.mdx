import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Stack, HStack, VStack, Card, Badge, Text } from '@cumulo/core';
import stackDoc from '../../../docgen/components/Stack.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Stack

Stack manages flexible layouts along horizontal or vertical axes with tokenized gaps, alignment, and wrapping.

```tsx
import { Stack, HStack, VStack } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview
  title="Horizontal and Vertical Stacks"
  description="Composable flexbox layouts adhering to Cumulo spacing tokens."
  code={`<VStack gap="md">
  <HStack gap="sm" align="center">
    <Badge variant="primary">Badge 1</Badge>
    <Badge variant="secondary">Badge 2</Badge>
    <Badge variant="outline">Badge 3</Badge>
  </HStack>
  <HStack gap="lg" justify="between" style={{ backgroundColor: 'var(--surface-bg-next)', padding: '12px', borderRadius: '6px' }}>
    <Text type="label">Left Item</Text>
    <Text type="label" color="muted">Right Item</Text>
  </HStack>
</VStack>`}
>
  <VStack gap="md">
    <HStack gap="sm" align="center">
      <Badge variant="primary">Badge 1</Badge>
      <Badge variant="secondary">Badge 2</Badge>
      <Badge variant="outline">Badge 3</Badge>
    </HStack>
    <HStack
      gap="lg"
      justify="between"
      style={{ backgroundColor: 'var(--surface-bg-next)', padding: '12px', borderRadius: '6px' }}
    >
      <Text type="label">Left Item</Text>
      <Text type="label" color="muted">
        Right Item
      </Text>
    </HStack>
  </VStack>
</ComponentPreview>

---

## Component API

<PropsTable data={stackDoc} />
