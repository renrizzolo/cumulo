import Layout, { CodeBlock, PropsTable, ComponentPreview } from '../../components/MDXLayout.tsx';
import { Input, VStack, HStack, Text } from '@cumulo/core';
import inputDoc from '../../../docgen/components/Input.json';
export default Layout;
export { CodeBlock, PropsTable, ComponentPreview };

# Input

Text inputs enable users to enter and edit text strings, credentials, and values.

```tsx
import { Input } from '@cumulo/core';
```

## Interactive Playground

<ComponentPreview
  title="Input Variants & States"
  description="Inputs automatically adapt background colors based on their containing surface."
  code={`<VStack gap="md" style={{ maxWidth: '360px' }}>
  <Input placeholder="Default input..." />
  <Input intent="success" defaultValue="Valid input value" />
  <Input intent="error" defaultValue="Invalid input value" />
  <Input size="small" placeholder="Small input..." />
  <Input disabled placeholder="Disabled input..." />
</VStack>`}
>
  <VStack gap="md" style={{ maxWidth: '360px' }}>
    <Input placeholder="Default input..." />
    <Input intent="success" defaultValue="Valid input value" />
    <Input intent="error" defaultValue="Invalid input value" />
    <Input size="small" placeholder="Small input..." />
    <Input disabled placeholder="Disabled input..." />
  </VStack>
</ComponentPreview>

---

## Component API

<PropsTable data={inputDoc} />
